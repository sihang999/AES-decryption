/*
 * aes.c - Implementation of AES (Advanced Encryption Standard) decryption functions
 *
 * This file contains the implementation of AES decryption operations and related lookup tables.
 * The focus is on the ECB (Electronic Codebook) mode of operation.
 *
 * Key features:
 * - Implements the AES decryption pipeline, including key expansion, inverse sub-byte 
 *   transformations, inverse mix columns, and shifting rows for decryption.
 * - Includes precomputed lookup tables such as S-box, inverse S-box, and multiplication tables 
 *   used in MixColumns and InverseMixColumns transformations.
 * - Adheres to the AES-128 standard with a 16-byte block size and 10 rounds of decryption.
 *
 * Functions:
 * - KeysExpansion(): Expands the original encryption key into multiple round keys.
 * - SubRoundKey(): Performs XOR operations to apply round keys.
 * - InverseMixColumns(): Reverses the MixColumns operation using lookup tables.
 * - ShiftRows(): Shifts rows of the state array to the right during decryption.
 * - SubBytes(): Performs substitution using the inverse S-box.
 * - Round(): Executes a complete round of decryption (excluding the initial and final rounds).
 * - InitialRound(): Executes the first decryption round.
 * - AES_ECB_decrypt(): Implements the overall AES decryption process for ECB mode.
 */

/*****************************************************************************/
/* Includes:                                                                 */
/*****************************************************************************/
#include <stdint.h>
#include "aes.h"


int mask_flag = 1;
int dummy_ops_flag = 1;
int shuffle_flag = 1;

/*****************************************************************************/
/* Functions for schuffling countermeasures                                                               */
/*****************************************************************************/

void shuffle_order(int *order, int size) {
    for (int i = size - 1; i > 0; i--) {
        int j = rand() % (i + 1); 
        int temp = order[i];
        order[i] = order[j];
        order[j] = temp;
    }
}

/*****************************************************************************/
/* Functions for Hiding countermeasures                                                               */
/*****************************************************************************/

// add rand time delays
void random_delay(void) {
    volatile int delay = rand() % 10; 
    for (int i = 0; i < delay; i++) {
    }
}

//add the useless opration
void inject_noise(void) {
    volatile int noise = rand() % 100;
	uint8_t fakeState[16], fakeKey[16];
	for (int i = 0; i < 16; i++) {
		fakeState[i] = (rand() % 256);
	}
	for (int i = 0; i < 16; i++) {
		fakeKey[i] = (rand() % 256);
	}
	int choice = rand() % 5;

    switch (choice) {
        case 0:
            SubRoundKey(fakeState, fakeKey);
            break;
        case 1:
            InverseMixColumns(fakeState);
            break;
        case 2:
            ShiftRows(fakeState);
            break;
        case 3:
            SubBytes_with_mask(fakeState, fakeKey, fakeKey);
            break;
		case 4:
			for (int i = 0; i < 10; i++) {
			noise += i * (noise >> 1);
			noise ^= (i * noise << 2);
			}
			break;
        default:
            break;
    }
    
}

/*****************************************************************************/
/* Functions for masking countermeasures                                                               */
/*****************************************************************************/

void initialize_masked_sbox(uint8_t mask_in, uint8_t mask_out, uint8_t *masked_sbox) {
    for (int i = 0; i < 256; i++) {
        masked_sbox[i] = inv_s[i ^ mask_in] ^ mask_out;
    }
}

void apply_mask(uint8_t *data, uint8_t *mask, int length) {
    for (int i = 0; i < length; i++) {
        data[i] ^= mask[i];
    }
}

void remove_mask(uint8_t *data, uint8_t *mask, int length) {
    for (int i = 0; i < length; i++) {
        data[i] ^= mask[i];
    }
}

void SubBytes_with_mask(uint8_t *state, uint8_t *mask_in, uint8_t *mask_out) {
	uint8_t masked_sbox[256];
	int order[4] = {0, 1, 2, 3}; 
	if(shuffle_flag){
		// Randomization order
    	shuffle_order(order, 4);
	}
    

	for (int row = 0; row < 4; row++) {
		//operate the random row
        int i = order[row]; 
        for (int col = 0; col < 4; col++) {
			initialize_masked_sbox(mask_in[i * 4 + col], mask_out[i * 4 + col], masked_sbox);
            state[i * 4 + col] = masked_sbox[state[i * 4 + col]];
        }
    }

}


// Encryption: Forward Rijndael S-box it is fixed, can not be changed!
// the non nagetive array including 256 values, each value from 0-255
const uint8_t s[256] =
{
	
};

// Used in KeyExpansion !round constant!
const uint8_t rcon[11] = {
	0x8d, 0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1b, 0x36
};

// Decryption: Inverse Rijndael S-box it is fixed, it cannot bechanged!
const uint8_t inv_s[256] =
{
	
};

// Decryption: Multiply by 9 for InverseMixColumns
const uint8_t mul9[256] =
{
	
};

// Decryption: Multiply by 11 for InverseMixColumns
const uint8_t mul11[256] =
{
	
};

// Decryption: Multiply by 13 for InverseMixColumns
const uint8_t mul13[256] =
{
	
};

// Decryption: Multiply by 14 for InverseMixColumns
const uint8_t mul14[256] =
{
	
};


/* ===================================================================
 * KeyExpansion
 * ====================================================================
 */

//Following three functions are the relative keyexpansion operations
void rotWord(uint8_t * in) {
	// Rotate left by one byte: shift left; in[0]=*(in + 0)
	uint8_t t = in[0];
	in[0] = in[1];
	in[1] = in[2];
	in[2] = in[3];
	in[3] = t;
}
void subWord(uint8_t * in) {
	// S-box 4 bytes 
	in[0] = s[in[0]];
	in[1] = s[in[1]];
	in[2] = s[in[2]];
	in[3] = s[in[3]];
}
void roundCon(uint8_t * in, uint8_t i) {
    // RCon xor operation,because the right 3 bytes of Rcon is zero! we just do xor with in[0]
	in[0] ^= rcon[i];
}

//Via this function you get 11x16bytes keys：expandedKeys[176]
void KeysExpansion(const uint8_t inputKey[16], uint8_t expandedKeys[176]) {
	// The first 128 bits are the original key
	for (int i = 0; i < 16; i++) {
		expandedKeys[i] = inputKey[i];
	}
	
	int bytesGenerated = 16; // Bytes we've generated so far
	int rconIteration = 1; // Keeps track of rcon value
	uint8_t lastWord1[4]; // The last word of expandedkeys
    uint8_t lastWord4[4]; // The fourth word from the end of expandedkeys

	while (bytesGenerated < 176) {
	    //The last word of expandedkeys
		for (int i = 0; i < 4; i++) {
			lastWord1[i] = expandedKeys[i + bytesGenerated - 4];
		}
      	//The fourth word from the end of expandedkeys
    	for (int i = 0; i < 4; i++) {
			lastWord4[i] = expandedKeys[i + bytesGenerated - 16];
		}
		// when excute the funtion using rconiteration, finsing the function rconiteration + 1
		if (bytesGenerated % 16 == 0) {
			rotWord(lastWord1);
			subWord(lastWord1);
			roundCon(lastWord1,rconIteration++);
		}
        // let the 4 bytes (12 bytes before) xor with now temCore!
		for (uint8_t a = 0; a < 4; a++) {
			expandedKeys[bytesGenerated] = lastWord4[a] ^ lastWord1[a];
			bytesGenerated++;
		}
	}
}
/* ===================================================================
 * SubRoundKey(used in round() function and last substract round key of decryption):
 * Substract the round key by do XOR operation again according to the xor opration laws.
 *====================================================================
 */
void SubRoundKey(uint8_t * state, uint8_t * roundKey) {
	for (int i = 0; i < 16; i++) {
		state[i] ^= roundKey[i];
	}
	if(dummy_ops_flag){
		// dummy operation	
		for (int i = 0; i < 1; i++) { 
			volatile uint8_t fake = inv_s[rand() % 256];
			fake = state[i]; 
		}
	}
	
}
/* ===================================================================
 * InverseMixColumns uses mul9, mul11, mul13, mul14 look-up tables
 * Unmixes the columns by reversing the effect of MixColumns in encryption
 *====================================================================
 */
void InverseMixColumns(uint8_t * state) {
	uint8_t tmp[16];
	int order[4] = {0, 1, 2, 3}; 
	if(shuffle_flag){
		// Randomization order
    	shuffle_order(order, 4);
	}

	for (int row = 0; row < 4; row++) {
        int i = order[row]; 

        tmp[i * 4 + 0] = (unsigned char)mul14[state[i * 4 + 0]] ^ mul11[state[i * 4 + 1]] ^ mul13[state[i * 4 + 2]] ^ mul9[state[i * 4 + 3]];
        tmp[i * 4 + 1] = (unsigned char)mul9[state[i * 4 + 0]] ^ mul14[state[i * 4 + 1]] ^ mul11[state[i * 4 + 2]] ^ mul13[state[i * 4 + 3]];
        tmp[i * 4 + 2] = (unsigned char)mul13[state[i * 4 + 0]] ^ mul9[state[i * 4 + 1]] ^ mul14[state[i * 4 + 2]] ^ mul11[state[i * 4 + 3]];
        tmp[i * 4 + 3] = (unsigned char)mul11[state[i * 4 + 0]] ^ mul13[state[i * 4 + 1]] ^ mul9[state[i * 4 + 2]] ^ mul14[state[i * 4 + 3]];
    }

	for (int i = 0; i < 16; i++) {
		state[i] = tmp[i];
	}

}
/* ===================================================================
 * Shifts rows right (rather than left,shift rows left is for encryption) for decryption
 *====================================================================
 */
void ShiftRows(uint8_t * state) {
	uint8_t tmp[16];
	int order[4] = {0, 1, 2, 3}; 
	if(shuffle_flag){
		// Randomization order
    	shuffle_order(order, 4);
	}

	for (int row = 0; row < 4; row++) {
    	int i = order[row];

        tmp[i * 4 + 0] = state[i * 4 + 0];
        tmp[i * 4 + 1] = state[(i * 4 + 1 + 12) % 16];
        tmp[i * 4 + 2] = state[(i * 4 + 2 + 8) % 16];
        tmp[i * 4 + 3] = state[(i * 4 + 3 + 4) % 16];
    }
	if(dummy_ops_flag){
		// dummy operation

		volatile uint8_t noise[16];
		for (int i = 0; i < 16; i++) {
			noise[i] = state[i] ^ (rand() % 256); 
		}
		for (int i = 0; i < 16; i++) {
			noise[i] ^= noise[(i + 1) % 16];
		}
	}


	for (int i = 0; i < 16; i++) {
		state[i] = tmp[i];
	}

}
/* ===================================================================
 * Perform substitution to each of the 16 bytes
 * Uses inverse S-box as lookup tabl
 *====================================================================
 */
void SubBytes(uint8_t * state) {

	int order[4] = {0, 1, 2, 3}; 
	// Randomization order
    shuffle_order(order, 4);

	for (int row = 0; row < 4; row++) {
		//operate the random row
        int i = order[row]; 
        for (int col = 0; col < 4; col++) {
            state[i * 4 + col] = inv_s[state[i * 4 + col]];
        }
    }

	if(dummy_ops_flag){
		// dummy operation
		for (int i = 0; i < 1; i++) { 
			volatile uint8_t fake = inv_s[rand() % 256];
			fake = state[i];
		}
	}
	

}
 /* ===================================================================
 * round 2nd-10th function, each round operates on 128 bits at a time
 * Corresponds to the 2nd-10th round of encryption.
 *====================================================================
 */
void Round(uint8_t * state, uint8_t * key, uint8_t *mask_in, uint8_t *mask_out, uint8_t round) {
	if(dummy_ops_flag && round==0){
		random_delay();
		inject_noise();
	}
	SubRoundKey(state, key);
	if(dummy_ops_flag && round==0){
		random_delay();
		inject_noise();
	}
	InverseMixColumns(state);
	if(dummy_ops_flag && round==0){
		random_delay();
		inject_noise();
	}
	ShiftRows(state);
	if(dummy_ops_flag && round==0){
		random_delay();
		inject_noise();
	}

	if(mask_flag){
		SubBytes_with_mask(state, mask_in, mask_out);
	}else{
		SubBytes(state);
	}
	
}
 /* ===================================================================
 * round 1st function(Same as Round function but no InverseMixColumns), each round operates on 128 bits at a time
 * Corresponds to the last round of encryption.
 *====================================================================
 */
void InitialRound(uint8_t * state, uint8_t * key, uint8_t *mask_in, uint8_t *mask_out) {
	SubRoundKey(state, key);
	ShiftRows(state);
	if(mask_flag){
		SubBytes_with_mask(state, mask_in, mask_out);
	}else{
		SubBytes(state);
	}
		
}
/* ===================================================================
 * The AES decryption function
 * Organizes all the decryption steps into one function
 *====================================================================
 */
void AES_ECB_decrypt(uint8_t * expandedKey, uint8_t * buf)
{
	uint8_t state[16]; // Stores the first 16 bytes of encrypted message
	uint8_t mask_in[16], mask_out[16];
    uint8_t ran1 = rand() % 256;
	uint8_t ran2 = rand() % 256;
	// generate the random mask
    for (int i = 0; i < 16; i++) {
        // mask_in[i] = rand() % 256;
		mask_in[i] = ran1;
        // mask_out[i] = rand() % 256;
		mask_out[i] = ran2;
    }

	// memcpy(state, buf, 16);
	for (int i = 0; i < 16; i++) {
		state[i] = buf[i];
	}
	if(mask_flag){
		apply_mask(state, mask_in, 16);//add mask for input data/state
	}
    
	//1st Round, We use the expandedKey[160] to expandedKey[175] to decryption
	InitialRound(state, expandedKey+160, mask_in, mask_out);
	if(mask_flag){
		remove_mask(state, mask_out, 16);
	}
	

	int numberOfRounds = 9;
	//Round 2nd to 10th(9 rounds), We use the expandedKey[16] to expandedKey[159] to decryption
	for (int i = 8; i >= 0; i--) {
		if(mask_flag){
			apply_mask(state, mask_in, 16);//add mask for input data/state
		}
		Round(state, expandedKey + (16 * (i + 1)), mask_in, mask_out,i);
		if(mask_flag){
			remove_mask(state, mask_out, 16);
		}
	}	

	//Final round, We use the expandedKey[0] to expandedKey[16] to decryption
	SubRoundKey(state, expandedKey); 

	// Copy decrypted state to buffer
	for (int i = 0; i < 16; i++) {
		buf[i] = state[i];
	}
}